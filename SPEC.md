# SPEC.md — soko-forge

<!-- scaffold template v1.7.1 から展開(2026-08-13)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

倉庫番(Sokoban)の「遊ぶ・解く・作る」を 1 つにしたブラウザアプリ。
レベル生成器が作った盤面をソルバーが必ず検証するため、**解けないレベルは決して出荷されない**。
コアロジック(エンジン / ソルバー / 生成器)は純関数として実装し、ハーネスエンジニアリング
(スペック駆動 + テスト先行 + ループ可観測性)の実践課題として最適な題材とする。

## 2. 機能要求

### 2.1 エンジン(loop_002)

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 盤面モデル: 壁 `#` / 床 ` ` / ゴール `.` / 箱 `$` / ゴール上の箱 `*` / プレイヤー `@` / ゴール上のプレイヤー `+` の標準 Sokoban テキスト記法をパース/シリアライズできる。不正入力(プレイヤー 0 or 2 人以上、箱数≠ゴール数、空盤面、非矩形は右パディング)はエラー種別付きで拒否する | must |
| F-02 | 移動: 上下左右 1 マス。移動先が床/ゴールなら移動、箱なら 1 個だけ押せる(押し先が床/ゴールのとき)。壁・箱 2 連・盤外へは不動(状態不変) | must |
| F-03 | クリア判定: 全箱がゴール上にあるとき solved=true | must |
| F-04 | undo / reset: 任意手数の undo で厳密に 1 手前の状態へ戻る。reset で初期盤面へ。手数(moves)と押し数(pushes)を数える(undo で巻き戻る) | must |
| F-05 | リプレイ: 手順を LURD 記法(押しは大文字 LURD、移動は小文字 lurd)で記録・再生できる。`applyMoves(level, "lurdLURD...")` は 1 手ずつの move と同一結果 | must |

### 2.2 ソルバー(loop_003)

| ID | 要求 | 優先度 |
|---|---|---|
| F-06 | ソルバー: BFS により**押し数最小**の解を求め、LURD 手順を返す。状態は(箱集合 × プレイヤー到達可能領域の正規化位置)でメモ化する | must |
| F-07 | デッドロック枝刈り: 静的デッドマス(ゴールのないコーナー、ゴールを含まない壁沿い区間)に箱を押し込む手を探索から除外する。デッドマス集合は前計算し公開する(UI 可視化用) | must |
| F-08 | 探索予算: 展開状態数の上限(既定 200,000)で打ち切り、結果を `solved` / `unsolvable` / `budget-exceeded` の 3 値で区別する。統計(展開数・生成数・経過は展開数で代理)を返す | must |

### 2.3 レベル生成器(loop_004)

| ID | 要求 | 優先度 |
|---|---|---|
| F-09 | 生成器: 逆再生(pull)方式。(1) 部屋テンプレートから壁形状を作る (2) ゴール位置に箱を置いた完成状態から、箱を**引く**操作をランダムに n 回適用して初期盤面を得る。乱数はシード付き PRNG を外部から注入する(同一シード → 同一レベル) | must |
| F-10 | 生成検証: 生成した盤面は必ずソルバーにかけ、`solved` 以外は棄却して再試行する。さらに最短押し数が **minPushes 未満の解は自明として棄却**する(minPushes は指定可、既定 `max(2, floor(pulls / 4))`)。最大試行回数内に得られなければ失敗を返す(例外にしない) | must |
| F-11 | 難易度スコア: 解の押し数・箱数・探索展開数から難易度 1..5 を算出する。単調性: 同一の他条件で押し数が増えればスコアは下がらない | must |

### 2.4 UI(loop_005)

| ID | 要求 | 優先度 |
|---|---|---|
| F-12 | プレイ画面: 矢印キー / WASD で操作、undo(Z / U)・reset(R)、手数・押し数表示、クリア時の演出と次レベル導線 | must |
| F-13 | 生成 UI: パラメータ(盤サイズ・箱数・シード・引き回数)を指定して生成。難易度と最短押し数を表示。シードを URL/表示で共有できる | must |
| F-14 | ソルバー可視化: 現在の盤面をソルバーで解き、解手順を 1 手ずつ自動再生できる(再生/停止)。デッドマスのオーバレイ表示 | should |
| F-15 | 内蔵レベル: 検証済みレベル 10 面以上を同梱し、レベル選択から遊べる。全内蔵レベルはテストでソルバー `solved` を保証する | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | `src/core/` は純関数のみ。React / DOM / Node API import 禁止、時間・乱数は注入 | テスト T-090(静的検査)+ レビュー |
| N-02 | `src/core` カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% | vitest coverage 閾値 |
| N-03 | Next.js 静的エクスポートで `next build` が成功する | `npm run verify` |
| N-04 | ソルバー性能: 内蔵全レベルを既定予算(200k 状態)内で解ける | テスト T-15x |
| N-05 | 生成器信頼性: 代表パラメータ 10 シードで全て検証済みレベルを生成できる | テスト T-14x |

## 4. 品質基準

- `npm run verify` green = typecheck(tsc エラー 0)+ lint(eslint エラー 0)+ test(全 green + N-02 閾値)+ build(静的エクスポート成功)
- 全ループが `harness/looplog.py validate` に合格していること

## 5. スコープ外

- オンライン機能(ランキング、レベル共有サーバ、アカウント)
- 手数最小(move-optimal)ソルバー(押し数最小のみ。手数はタイブレークしない)
- レベルエディタ(手描き編集 UI)
- モバイル専用 UI 最適化(タップ移動は best-effort)
- i18n(UI は日本語のみ)
