# TEST_SPEC.md — soko-forge

<!-- scaffold template v1.7.1 から展開(2026-08-13) -->

## 実行規約

- `npx vitest run` を stage 3–5 の判定に使用。完了条件は `npm run verify`(typecheck + lint + test + build)
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャ(最短押し数などの期待値)は、期待値の導出前提を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)
- 乱数を使うテストは必ず固定シードで行う(VERIF-FLAKE の予防)

## ケース一覧

### loop_001 立ち上げ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-000 | N-03 | プロジェクトメタ(APP_NAME)が core から取得できる | "soko-forge" |

### loop_002 エンジン

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 | 標準記法のレベルをパースし再シリアライズ | ラウンドトリップ一致(右パディング正規化後) |
| T-002 | F-01 | プレイヤー 0 人 / 2 人 | エラー種別 no-player / multiple-players |
| T-003 | F-01 | 箱数≠ゴール数 | エラー種別 box-goal-mismatch |
| T-004 | F-01 | `*`(ゴール上の箱)と `+`(ゴール上のプレイヤー)の解釈 | 箱・ゴール・プレイヤー位置が正しい |
| T-010 | F-02 | 床への移動 / 壁への移動 | 移動する / 状態不変 |
| T-011 | F-02 | 箱押し(押し先が床・ゴール) | 箱とプレイヤーが 1 マス進む |
| T-012 | F-02 | 箱押し不可(壁・箱 2 連) | 状態不変 |
| T-020 | F-03 | 全箱ゴールで solved | true(それ以外 false) |
| T-030 | F-04 | move → undo | 初期状態と厳密一致、moves/pushes も巻き戻る |
| T-031 | F-04 | 複数手 undo と reset | 各時点の状態と一致 / 初期化 |
| T-040 | F-05 | LURD 記録と applyMoves 再生 | 逐次 move と同一状態、大文字=押し |

### loop_003 ソルバー

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-100 | F-06 | 自明レベル(箱 1・直線)の最短解 | 押し数 = 手計算値(導出コメント付き) |
| T-101 | F-06 | 既知レベル(microban 級)の最短押し数 | 手計算/既知値と一致 |
| T-102 | F-06 | 解の再生 | applyMoves(解) が solved |
| T-110 | F-07 | ゴールなしコーナーがデッドマス | deadSquares に含まれる |
| T-111 | F-07 | 解けない盤面(コーナーに箱) | unsolvable |
| T-120 | F-08 | 予算 1 で複雑盤面 | budget-exceeded、統計が返る |

### loop_004 生成器

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-140 | F-09 | 同一シードで 2 回生成 | 同一レベル(決定性) |
| T-141 | F-10 | 生成レベルをソルバー検証 | 全て solved かつ押し数 ≥ 1 |
| T-142 | N-05 | 代表パラメータ × 10 シード | 全て生成成功 |
| T-145 | F-10 | 既定 minPushes(代表パラメータ pulls=12 → 3) | 全生成レベルの押し数 ≥ 3 |
| T-146 | F-10 | 明示指定 minPushes=5 | 押し数 ≥ 5(成功時)。5 未満の解が採用されない |
| T-143 | F-11 | 難易度の単調性(押し数増でスコア非減) | 単調性成立 |
| T-144 | F-11 | 難易度の値域 | 1..5 の整数 |

### loop_005 UI / データ

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-150 | F-15 | 内蔵全レベルがパース可能 | エラーなし |
| T-151 | F-15, N-04 | 内蔵全レベルがソルバーで solved(既定予算内) | solved |
| T-090 | N-01 | src/core 内に react/next/dom/Date.now/Math.random の参照がない | 静的検査 pass |
